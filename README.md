# LibChemView

This is a very simple library for showing molecules in a standard matplotlib environment. It works best by adding the molecule to a separate axis with invisible lines. Useful for adding molecules directly to graphs or keeping the same scale whilst drawing multiple molecules.

See Example.py for examples of use
